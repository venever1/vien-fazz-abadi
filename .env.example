# Frontend (Vite) — production API base URL
# Wajib diisi saat build untuk deployment (Cloudflare Pages / production).
# Contoh: VITE_API_URL=https://api-xxx.onrender.com/api
# Jika kosong, fallback ke http://localhost:3001/api (development).
VITE_API_URL=
